import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Hilo de WhatsApp resuelto por el agente. Conversacion ilustrativa. */
@Component({
  selector: 'app-agent-artifact',
  templateUrl: './agent-artifact.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AgentArtifact {}
